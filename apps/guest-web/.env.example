# Browser-reachable backend API
NEXT_PUBLIC_API_URL=http://localhost:3001/api
# demo.restra.localhost in development; demo.restra.com in production
NEXT_PUBLIC_TENANT_ROOT_DOMAIN=restra.localhost
# Preview-only escape hatch; leave unset/false on the production guest project.
ALLOW_NON_TENANT_HOSTS=false
